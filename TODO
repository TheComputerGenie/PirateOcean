/**
 * @todo Add z bits
 * @body Need to use key(s) to view tx and populate data.
 */
/**
 * @todo Complete donation button
 * @body Currently, the button is just there, it needs code to add the recipient addy and amount.
 */
/**
 * @todo Correct "Use available balance"
 * @body tx fee is not properly subtracted.
 */
